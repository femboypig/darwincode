# darwincode TODO

### 1. Валидация API ключа
- [ ] Проверка префикса (`AIza...` для Gemini, `sk-` для OpenAI)
- [ ] Проверка длины (минимум 20 символов)
- [ ] Базовый regex: `^[A-Za-z0-9_-]{20,}$`

### 2. Обработка сигналов (SIGINT/SIGTERM)
- [ ] Добавить `ctrlc` crate
- [ ] Корректно завершать работу (отключить raw mode, вернуть терминал)

### 3. Копирование текста
- [ ] `Ctrl+Shift+C` — копировать выделенное

### 4. Undo для ввода
- [ ] Добавить историю изменений input (максимум 50 шагов)

### 6. Замена `grep` на кроссплатформенное решение
- [ ] Использовать `regex` + `walkdir` вместо `std::process::Command::new("grep")`

### 7. Оптимизация перерисовки чата
- [ ] Обновлять только измененные строки
- [ ] Рассмотреть `ratatui::widgets::Table` вместо `Paragraph`

### 8. Документация
- [ ] Добавить `rustdoc` комментарии к публичным структурам и функциям

### 10. Windows и macOS поддержка
- [ ] Добавить поддержку Windows (keyring, machine-id)
- [ ] Добавить поддержку macOS (keyring, machine-id)

### 11. Fallback для API ключа
- [ ] Если keyring недоступен — читать из файла
- [ ] Если и там пусто — показывать экран настройки

## Не утверждено (требует обсуждения)

### Архитектура
- [ ] Рефакторинг `App` на `ScreenManager`, `InputHandler`, `APIClient`
- [ ] Интерфейс для "плагинов" (кастомные инструменты)

### Производительность
- [ ] Оптимизация шифрования сессий (не каждый файл отдельно)

### Безопасность
- [ ] Проверка `grep` аргументов на инъекции
